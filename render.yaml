services:
  - type: web
    name: imi-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGO_URI
        sync: false        # Set this in Render Dashboard → Environment
      - key: JWT_SECRET
        sync: false        # Set this in Render Dashboard → Environment
      - key: ZAAKPAY_MERCHANT_IDENTIFIER
        sync: false
      - key: ZAAKPAY_SECRET_KEY
        sync: false
      - key: ZAAKPAY_BASE_URL
        value: https://zaakpay.com
      - key: BACKEND_URL
        sync: false        # Set to your Render service URL e.g. https://imi-backend.onrender.com
      - key: FRONTEND_URL
        sync: false        # Set to your frontend URL
